[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "venvdoctor"
version = "0.1.0"
description = "Analyze Python virtual environments"
readme = "README.md"
requires-python = ">=3.9"

[project.scripts]
venvdoctor = "venvdoctor.cli:main"